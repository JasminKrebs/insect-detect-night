# Required Python packages for the Insect Detect Night DIY camera trap (Raspberry Pi Zero 2 W + OAK-1)

# Source:   https://github.com/JasminKrebs/insect-detect-night
# License:  GNU GPLv3 (https://choosealicense.com/licenses/gpl-3.0/)
# Author:   Jasmin Krebs (https://github.com/JasminKrebs)
# Docs:     https://maxsitt.github.io/insect-detect-docs/

# Use Raspberry Pi Imager to set up the microSD card with:
# Raspberry Pi OS Lite (32-bit) based on Debian 12 Bookworm (Python 3.11)

# Update the already installed software and reboot:
# sudo apt update && sudo apt full-upgrade
# sudo reboot

# Install software & dependencies, clone github repo: -> don't do this
# wget -qO- https://raw.githubusercontent.com/JasminKrebs/insect-detect-night/main/insect_detect_install.sh | sudo bash

# do instead:
# nano s.sh
# then copy all the contents from https://github.com/JasminKrebs/insect-detect-night/blob/main/insect_detect_install.sh
# save (ctrl+o then enter to save; then ctrl+x to exit)
# chmod +x s.sh
# ./s.sh
# s.sh

# To enable LED from startup:
# sudo nano /etc/systemd/system/led-server.service
# copy the following there:
  #[Unit]
  #Description=LED Server for Insect Detect
  #After=network.target
  
  #[Service]
  #ExecStart=/home/pi/env_insdet/bin/python3 /home/pi/insect-detect-night/utils/led.py
  #WorkingDirectory=/home/pi/insect-detect-night/utils
  #Restart=always
  #User=root
  
  #[Install]
  #WantedBy=multi-user.target

# then: ctrl+o then enter to save; then ctrl+x to exit

# then:
  # sudo systemctl daemon-reload
  # sudo systemctl enable led-server
  # sudo systemctl start led-server
# sudo reboot

# Optional: Install and configure Rclone (https://rclone.org/docs/) if you want to use the upload feature:
# wget -qO- https://rclone.org/install.sh | sudo bash

# Run the scripts with the Python interpreter from the virtual environment:
# env_insdet/bin/python3 insect-detect-night/webapp.py

# Debian 12 Bookworm (Python 3.11)
depthai==2.30.0.0
opencv-python==4.7.0.72
numpy==1.26.4
nicegui==2.22.1
pyyaml==6.0.2
ruamel.yaml==0.18.14
nmcli==1.5.0
pandas==2.2.3
psutil==7.0.0
apscheduler==3.11.0
av==13.0.0
gpiozero==2.0.1
smbus2==0.4.2
rpi_ws281x==5.0.0
adafruit_blinka==8.62.0
adafruit_circuitpython_tsl2591==1.4.4
fastapi==0.116.1

# Debian 11 Bullseye (Python 3.9)
#opencv-python==4.6.0.66
#av==10.0.0
